# 2026-03-24 Daily Memory

## Shopee Exam Study Session
- User studied Shopee exam questions via image submissions
- Covered topics: anti-fraud rules, SLS shipping, inventory management, coupon settings, penalty system
- Most answers analyzed correctly; some corrections noted for wrong answers

## Technical Fixes & Deployments

### 1. COS Storage Path Fix
**Problem:** Files uploaded with nested structure like `main_images/main_00.jpg/main_00.jpg`
**Root Cause:** rclone `copy` command behavior
**Fix:** Changed to `copyto` command in `cos_storage.py`
**File:** `/home/ubuntu/.openclaw/skills/shared/cos_storage.py`

### 2. SSH Tunnel Port Changed
**Change:** local-1688-weight tunnel now uses port **8080** (was 9090)
**Config file:** `/root/.openclaw/workspace-e-commerce/skills/local-1688-weight/scripts/remote_weight_caller.py`
**Health check:** `curl http://127.0.0.1:8080/health`

### 3. product-storer Enhancements
**Files modified:**
- `/home/ubuntu/.openclaw/skills/product-storer/storer.py`

**Changes:**
- `store()` now accepts `weight_data` parameter and calls `store_skus()`
- Fixed SKU field mapping (handles `name` field from collector-scraper)
- Added `_upload_images_to_cos()` with new directory structure:
  ```
  /{product_id_new}_{title(50bytes)}/
  ├── main_images/
  ├── sku_images/
  └── detail_images/
  ```
- Removed image upload limit (was [:10] for main_images)

### 4. collector-scraper Improvements
**File:** `/home/ubuntu/.openclaw/skills/collector-scraper/scraper.py`

**Changes:**
- Added `detail_images` field
- Description extraction fixed (lowered threshold from >50 to >10 chars)
- Sidebar thumbnail filtering added (filters `-0-cib` pattern URLs)
- SKU extraction enhanced with JavaScript:
  - Price extraction: `.jx-pro-input.price-input input.el-input__inner`
  - Stock extraction: `.jx-pro-input input.el-input__inner` (non-price)
  - Stock deduplication: filter values appearing only once (true SKU stock vs repeated defaults)
  - Color image: `.el-image.img-box .el-image__inner` selector

### 5. Image Upload Limit Removed
- Was: 10 main images max
- Now: unlimited (all uploaded)

## Final Test Results (2026-03-24 Evening)

### SKU Extraction - SUCCESS
| Data | Value |
|------|-------|
| SKU Name | ✅ 奶白色、奶黄色 |
| Price | ✅ 12.9 |
| Stock | ✅ 2220、2218 |
| Weight | ✅ 2500g (from local-1688-weight) |
| SKU Image | ❌ None (DOM complexity - colors not in .el-image.img-box) |

### Data Flow
```
collector-scraper → remote_weight_caller → product-storer → COS
     ✅                ✅                    ✅            ✅
```

## Current Issues to Address

1. **SKU Image Extraction**: DOM structure too complex - color names "奶白色/奶黄色" not found in `.el-image.img-box` container. Found 38 `.el-image__inner` elements but names are尺寸, not colors. Needs further debugging.

## Test Product
- URL: https://detail.1688.com/offer/1026175430866.html
- Product ID in DB: 18
- Main product ID: 一000001 / AL0001001260000001
- Main images: 21 (filtered from 29, removed sidebar thumbnails)

## Database Schema Notes

### products table
- `product_id` = Chinese format like 一000001 (old, human-readable)
- `product_id_new` = AL0001001260000001 (18-digit new format)

### product_skus table
- Fields: id, product_id, sku_name, color, size, price, stock, package_weight, sku_id_new
- Weight correctly written from local-1688-weight (e.g., 2500g)
- Price/stock now correctly extracted by JavaScript
